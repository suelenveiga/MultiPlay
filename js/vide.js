const input = document.querySelector('input');
const a = document.querySelector('a');

a.addEventListener('click',function(){
    URL = `https://www.google.com.br/`
        
        var p =document.createElement('p');
        div.textContent = URL;
        postagens.appendChild(div);
    
});